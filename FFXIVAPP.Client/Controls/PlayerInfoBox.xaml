<UserControl x:Class="FFXIVAPP.Client.Controls.PlayerInfoBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:CommonConverters="clr-namespace:FFXIVAPP.Common.Converters;assembly=FFXIVAPP.Common"
             xmlns:Custom="http://metro.mahapps.com/winfx/xaml/shared"
             xmlns:Parse="clr-namespace:FFXIVAPP.Client.ViewModels.Plugins.Parse"
             xmlns:PluginProperties="clr-namespace:FFXIVAPP.Client.SettingsProviders.Parse"
             xmlns:Properties="clr-namespace:FFXIVAPP.Client.Properties"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <CommonConverters:VisibilityConverter x:Key="VisibilityConverter" />
        <x:Static x:Key="PluginSettings"
                  Member="PluginProperties:Settings.Default" />
        <Custom:ToUpperConverter x:Key="ToUpperConverter" />
        <x:Static x:Key="PluginViewModel"
                  Member="Parse:PluginViewModel.Instance" />
        <CommonConverters:NameToAvatarConverter x:Key="NameToAvatarConverter" />
        <Style x:Key="StatLabelStyle">
            <Setter Property="Control.Padding" Value="0" />
            <Setter Property="FrameworkElement.HorizontalAlignment" Value="Left" />
        </Style>
        <Style x:Key="CommaNumberStyle"
               BasedOn="{StaticResource StatLabelStyle}">
            <Setter Property="ContentControl.ContentStringFormat" Value="{}{0:N0}" />
        </Style>
        <Style x:Key="PercentStyle"
               BasedOn="{StaticResource StatLabelStyle}">
            <Setter Property="ContentControl.ContentStringFormat" Value="{}{0:P}" />
        </Style>
        <Style x:Key="WhiteStatTextBlockStyle">
            <Setter Property="Control.Padding" Value="0" />
            <Setter Property="Control.Foreground" Value="White" />
            <Setter Property="FrameworkElement.HorizontalAlignment" Value="Left" />
        </Style>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot"
          Width="270"
          Margin="5">
        <Grid.BitmapEffect>
            <BitmapEffectGroup>
                <BlurBitmapEffect x:Name="GaussianBlur"
                                  KernelType="Gaussian"
                                  Radius="10" />
            </BitmapEffectGroup>
        </Grid.BitmapEffect>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard x:Name="LayoutRootBeginStoryBoard">
                    <Storyboard x:Name="LayoutRootStoryBoard">
                        <DoubleAnimation AutoReverse="false"
                                         BeginTime="0:0:0"
                                         Duration="0:0:.5"
                                         From="0"
                                         Storyboard.TargetName="LayoutRoot"
                                         Storyboard.TargetProperty="(UIElement.Opacity)"
                                         To="1" />
                        <DoubleAnimation AutoReverse="false"
                                         BeginTime="0:0:0"
                                         Duration="0:0:.5"
                                         From="10"
                                         Storyboard.TargetName="GaussianBlur"
                                         Storyboard.TargetProperty="(Radius)"
                                         To="0" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="40" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="5" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5" />
            <RowDefinition Height="5" />
            <RowDefinition Height="20" />
            <RowDefinition Height="25" />
            <RowDefinition Height="*" />
            <RowDefinition Height="5" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0"
                Grid.RowSpan="3"
                Grid.Column="0"
                Grid.ColumnSpan="5"
                Background="{DynamicResource AccentColorBrush}"
                BorderBrush="{DynamicResource AccentColorBrush}"
                BorderThickness="1" />
        <Image Grid.Row="1"
               Grid.RowSpan="3"
               Grid.Column="2"
               Width="40"
               Height="40"
               RenderOptions.BitmapScalingMode="HighQuality">
            <Image.Source>
                <MultiBinding Converter="{StaticResource NameToAvatarConverter}">
                    <Binding RelativeSource="{RelativeSource Self}" />
                    <Binding Path="Name" />
                </MultiBinding>
            </Image.Source>
            <Image.Effect>
                <DropShadowEffect BlurRadius="3"
                                  Direction="0"
                                  RenderingBias="Quality"
                                  ShadowDepth="0" />
            </Image.Effect>
        </Image>
        <DockPanel Grid.Row="0"
                   Grid.RowSpan="3"
                   Grid.Column="3"
                   LastChildFill="True">
            <DockPanel DockPanel.Dock="Right">
                <TextBlock Margin="5 0"
                           VerticalAlignment="Center"
                           FontSize="20"
                           Style="{StaticResource WhiteStatTextBlockStyle}"
                           Text="{Binding DPS,
                                          StringFormat=\{0:N0\}}" />
                <TextBlock Margin="4"
                           VerticalAlignment="Bottom"
                           Padding="0"
                           Style="{StaticResource WhiteStatTextBlockStyle}"
                           Text="DPS" />
            </DockPanel>
            <DockPanel />
        </DockPanel>
        <Label Grid.Row="3"
               Grid.Column="3"
               VerticalAlignment="Center"
               Content="{Binding Name,
                                 Converter={StaticResource ToUpperConverter}}"
               Foreground="{DynamicResource LabelTextBrush}"
               Padding="5 0" />
        <Border Grid.Row="3"
                Grid.Column="0"
                Grid.ColumnSpan="5"
                BorderBrush="{DynamicResource AccentColorBrush}"
                BorderThickness="1 0" />
        <Border Grid.Row="4"
                Grid.RowSpan="3"
                Grid.Column="0"
                Grid.ColumnSpan="5"
                BorderBrush="{DynamicResource AccentColorBrush}"
                BorderThickness="1 0 1 1">
            <Grid Margin="5">
                <DockPanel LastChildFill="True">
                    <DockPanel DockPanel.Dock="Left">
                        <StackPanel x:Name="StatNamesPanel"
                                    Grid.Column="0"
                                    HorizontalAlignment="Left">
                            <Label Content="{Binding Locale[parse_TotalOverallDamageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicTotalOverallDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_RegularDamageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicRegularDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_CriticalDamageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicCriticalDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_TotalDamageActionsUsedLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicTotalDamageActionsUsed,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DPSLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDPS,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageDOTLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageDOT,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageDOTAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageDOTAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageRegHitLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageRegHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageRegMissLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageRegMiss,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageRegAccuracyLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageRegAccuracy,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageRegLowLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageRegLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageRegHighLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageRegHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageRegAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageRegAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageRegModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageRegMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageRegModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageRegModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageCritHitLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageCritHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageCritPercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageCritPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageCritLowLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageCritLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageCritHighLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageCritHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageCritAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageCritAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageCritModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageCritMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageCritModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageCritModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageCounterLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageCounter,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageCounterPercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageCounterPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageCounterModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageCounterMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageCounterModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageCounterModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageBlockLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageBlock,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageBlockPercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageBlockPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageBlockModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageBlockMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageBlockModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageBlockModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageParryLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageParry,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageParryPercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageParryPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageParryModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageParryMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageParryModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageParryModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageResistLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageResist,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageResistPercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageResistPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageResistModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageResistMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageResistModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageResistModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageEvadeLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageEvade,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageEvadePercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageEvadePercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageEvadeModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageEvadeMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageEvadeModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageEvadeModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_PercentOfTotalOverallDamageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicPercentOfTotalOverallDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_PercentOfRegularDamageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicPercentOfRegularDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_PercentOfCriticalDamageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicPercentOfCriticalDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_TotalOverallHealingLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicTotalOverallHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_RegularHealingLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicRegularHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_CriticalHealingLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicCriticalHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_TotalHealingActionsUsedLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicTotalHealingActionsUsed,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HPSLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHPS,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingRegHitLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingRegHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingRegLowLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingRegLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingRegHighLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingRegHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingRegAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingRegAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingRegModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingRegMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingRegModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingRegModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingCritHitLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingCritHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingCritPercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingCritPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingCritLowLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingCritLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingCritHighLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingCritHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingCritAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingCritAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingCritModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingCritMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_HealingCritModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicHealingCritModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_PercentOfTotalOverallHealingLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicPercentOfTotalOverallHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_PercentOfRegularHealingLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicPercentOfRegularHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_PercentOfCriticalHealingLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicPercentOfCriticalHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_TotalOverallDamageTakenLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicTotalOverallDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_RegularDamageTakenLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicRegularDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_CriticalDamageTakenLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicCriticalDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_TotalDamageTakenActionsUsedLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicTotalDamageTakenActionsUsed,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DTPSLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDTPS,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenDOTLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenDOT,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenDOTAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenDOTAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenRegHitLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenRegMissLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegMiss,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenRegAccuracyLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegAccuracy,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenRegLowLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenRegHighLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenRegAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenRegModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenRegModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenCritHitLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenCritPercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenCritLowLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenCritHighLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenCritAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenCritModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenCritModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenCounterLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCounter,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenCounterPercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCounterPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenCounterModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCounterMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenCounterModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCounterModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenBlockLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenBlock,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenBlockPercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenBlockPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenBlockModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenBlockMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenBlockModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenBlockModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenParryLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenParry,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenParryPercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenParryPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenParryModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenParryMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenParryModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenParryModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenResistLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenResist,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenResistPercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenResistPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenResistModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenResistMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenResistModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenResistModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenEvadeLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenEvade,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenEvadePercentLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenEvadePercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenEvadeModLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenEvadeMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_DamageTakenEvadeModAverageLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenEvadeModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_PercentOfTotalOverallDamageTakenLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicPercentOfTotalOverallDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_PercentOfRegularDamageTakenLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicPercentOfRegularDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding Locale[parse_PercentOfCriticalDamageTakenLabel],
                                                     Source={StaticResource AppProperties}}"
                                   Style="{StaticResource StatLabelStyle}"
                                   Visibility="{Binding ShowBasicPercentOfCriticalDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                        </StackPanel>
                    </DockPanel>
                    <DockPanel>
                        <StackPanel Grid.Column="1"
                                    Height="Auto"
                                    HorizontalAlignment="Right">
                            <Label Content="{Binding TotalOverallDamage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicTotalOverallDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding RegularDamage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicRegularDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding CriticalDamage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicCriticalDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding TotalDamageActionsUsed}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicTotalDamageActionsUsed,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DPS}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDPS,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageDOT}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageDOT,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageDOTAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageDOTAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageRegHit}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageRegHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageRegMiss}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageRegMiss,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageRegAccuracy}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageRegAccuracy,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageRegLow}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageRegLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageRegHigh}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageRegHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageRegAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageRegAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageRegMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageRegMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageRegModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageRegModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageCritHit}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageCritHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageCritPercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageCritPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageCritLow}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageCritLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageCritHigh}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageCritHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageCritAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageCritAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageCritMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageCritMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageCritModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageCritModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageCounter}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageCounter,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageCounterPercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageCounterPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageCounterMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageCounterMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageCounterModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageCounterModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageBlock}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageBlock,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageBlockPercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageBlockPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageBlockMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageBlockMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageBlockModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageBlockModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageParry}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageParry,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageParryPercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageParryPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageParryMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageParryMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageParryModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageParryModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageResist}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageResist,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageResistPercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageResistPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageResistMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageResistMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageResistModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageResistModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageEvade}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageEvade,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageEvadePercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageEvadePercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageEvadeMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageEvadeMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageEvadeModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageEvadeModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding PercentOfTotalOverallDamage}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicPercentOfTotalOverallDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding PercentOfRegularDamage}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicPercentOfRegularDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding PercentOfCriticalDamage}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicPercentOfCriticalDamage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding TotalOverallHealing}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicTotalOverallHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding RegularHealing}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicRegularHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding CriticalHealing}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicCriticalHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding TotalHealingActionsUsed}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicTotalHealingActionsUsed,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HPS}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHPS,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingRegHit}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingRegHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingRegLow}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingRegLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingRegHigh}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingRegHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingRegAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingRegAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingRegMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingRegMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingRegModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingRegModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingCritHit}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingCritHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingCritPercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicHealingCritPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingCritLow}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingCritLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingCritHigh}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingCritHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingCritAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingCritAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingCritMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingCritMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding HealingCritModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicHealingCritModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding PercentOfTotalOverallHealing}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicPercentOfTotalOverallHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding PercentOfRegularHealing}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicPercentOfRegularHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding PercentOfCriticalHealing}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicPercentOfCriticalHealing,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding TotalOverallDamageTaken}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicTotalOverallDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding RegularDamageTaken}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicRegularDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding CriticalDamageTaken}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicCriticalDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding TotalDamageTakenActionsUsed}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicTotalDamageTakenActionsUsed,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DTPS}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDTPS,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenDOT}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenDOT,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenDOTAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenDOTAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenRegHit}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenRegMiss}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegMiss,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenRegAccuracy}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegAccuracy,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenRegLow}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenRegHigh}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenRegAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenRegMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenRegModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenRegModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenCritHit}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritHit,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenCritPercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenCritLow}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritLow,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenCritHigh}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritHigh,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenCritAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenCritMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenCritModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCritModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenCounter}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCounter,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenCounterPercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCounterPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenCounterMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCounterMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenCounterModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenCounterModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenBlock}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenBlock,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenBlockPercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenBlockPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenBlockMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenBlockMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenBlockModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenBlockModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenParry}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenParry,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenParryPercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenParryPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenParryMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenParryMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenParryModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenParryModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenResist}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenResist,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenResistPercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenResistPercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenResistMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenResistMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenResistModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenResistModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenEvade}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenEvade,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenEvadePercent}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenEvadePercent,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenEvadeMod}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenEvadeMod,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding DamageTakenEvadeModAverage}"
                                   Style="{StaticResource CommaNumberStyle}"
                                   Visibility="{Binding ShowBasicDamageTakenEvadeModAverage,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding PercentOfTotalOverallDamageTaken}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicPercentOfTotalOverallDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding PercentOfRegularDamageTaken}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicPercentOfRegularDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                            <Label Content="{Binding PercentOfCriticalDamageTaken}"
                                   Style="{StaticResource PercentStyle}"
                                   Visibility="{Binding ShowBasicPercentOfCriticalDamageTaken,
                                                        Source={StaticResource PluginSettings},
                                                        Converter={StaticResource VisibilityConverter}}" />
                        </StackPanel>
                    </DockPanel>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>